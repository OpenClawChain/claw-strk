# claw-strk default config
#
# For global installs (npm -g), the default location is:
#   ~/.claw-strk/.env
#
# You can also run from a project folder with a local .env,
# or pass an explicit path:
#   claw-strk --env /path/to/.env <command>

# Required
STARKNET_ACCOUNT_ADDRESS=0x...
STARKNET_PRIVATE_KEY=0x...

# Optional
STARKNET_RPC_URL=https://starknet-sepolia-rpc.publicnode.com
